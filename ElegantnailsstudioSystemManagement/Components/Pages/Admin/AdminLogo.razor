@page "/admin/logo"
@layout AdminLayout
@using System.IO
@inject ILogoService LogoService
@inject NavigationManager Navigation

<PageTitle>Elegant Nails - Logo</PageTitle>

<div class="container mt-4">
    <h1 class="mb-4"><i class="bi bi-palette me-2"></i>Gestión del Logo</h1>

    @if (!string.IsNullOrEmpty(mensajeError))
    {
        <div class="alert alert-danger alert-dismissible fade show">
            <i class="bi bi-exclamation-triangle me-2"></i>
            @mensajeError
            <button type="button" class="btn-close" @onclick="() => mensajeError = null"></button>
        </div>
    }

    @if (!string.IsNullOrEmpty(mensajeExito))
    {
        <div class="alert alert-success alert-dismissible fade show">
            <i class="bi bi-check-circle me-2"></i>
            @mensajeExito
            <button type="button" class="btn-close" @onclick="() => mensajeExito = null"></button>
        </div>
    }

    <div class="row">
        <!-- Logo Actual -->
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-image me-2"></i>Logo Actual</h5>
                </div>
                <div class="card-body text-center">
                    @if (logoActual != "/images/Logo.jpg")
                    {
                        <div class="mb-3">
                            <img src="@logoActual"
                                 alt="Logo Elegant Nails"
                                 class="img-fluid rounded border p-2 bg-light"
                                 style="max-height: 200px; max-width: 100%;" />
                        </div>
                        <div class="alert alert-info small">
                            <i class="bi bi-info-circle me-1"></i>
                            Logo personalizado cargado
                        </div>
                        <button class="btn btn-danger btn-sm" @onclick="MostrarModalEliminar">
                            <i class="bi bi-trash me-1"></i>Eliminar Logo
                        </button>
                    }
                    else
                    {
                        <div class="py-4">
                            <i class="bi bi-image text-muted display-4 d-block mb-3"></i>
                            <h5 class="text-muted">Logo por defecto</h5>
                            <p class="text-muted small mb-0">Sube un logo para personalizar tu sitio</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Subir Nuevo Logo -->
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0"><i class="bi bi-cloud-arrow-up me-2"></i>Subir Nuevo Logo</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Seleccionar archivo de imagen</label>

                        <!-- Formulario de subida TRADICIONAL para Blazor Server -->
                        <form method="post" enctype="multipart/form-data" @onsubmit="SubirLogo">
                            <div class="mb-3">
                                <input type="file"
                                       class="form-control"
                                       name="archivoLogo"
                                       accept=".png,.jpg,.jpeg,.gif,.webp"
                                       @onchange="HandleFileSelected" />
                                <div class="form-text text-muted">
                                    <i class="bi bi-info-circle me-1"></i>
                                    Formatos permitidos: PNG, JPG, JPEG, GIF, WEBP (Máximo 5MB)
                                </div>
                            </div>

                            <!-- Vista previa -->
                            @if (!string.IsNullOrEmpty(previewImagen))
                            {
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Vista previa:</label>
                                    <div class="text-center">
                                        <img src="@previewImagen"
                                             alt="Vista previa"
                                             class="img-thumbnail border-success"
                                             style="max-height: 150px; max-width: 100%;" />
                                        <div class="mt-2 small">
                                            <span class="badge bg-info">
                                                <i class="bi bi-file-earmark-image me-1"></i>
                                                @fileName
                                            </span>
                                            <span class="badge bg-secondary ms-2">
                                                <i class="bi bi-hdd me-1"></i>
                                                @fileSize KB
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            }

                            <!-- Botón Subir -->
                            <div class="d-grid gap-2">
                                <button type="submit"
                                        class="btn btn-success"
                                        disabled="@(archivoStream == null || isUploading)">
                                    @if (isUploading)
                                    {
                                        <span>
                                            <span class="spinner-border spinner-border-sm me-2" role="status"></span>
                                            Subiendo...
                                        </span>
                                    }
                                    else
                                    {
                                        <span><i class="bi bi-upload me-2"></i>Subir Logo</span>
                                    }
                                </button>

                                <button type="button" class="btn btn-outline-secondary" @onclick="Cancelar">
                                    <i class="bi bi-x-circle me-2"></i>Cancelar
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal de Confirmación -->
@if (mostrarModalEliminar)
{
    <div class="modal fade show d-block" style="background-color: rgba(0,0,0,0.5);" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title">
                        <i class="bi bi-exclamation-triangle me-2"></i>Confirmar Eliminación
                    </h5>
                    <button type="button" class="btn-close btn-close-white" @onclick="CerrarModal"></button>
                </div>
                <div class="modal-body text-center">
                    <i class="bi bi-trash text-danger display-4 d-block mb-3"></i>
                    <h4>¿Eliminar el logo actual?</h4>
                    <p class="text-muted">Se restaurará el logo por defecto.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="CerrarModal">
                        <i class="bi bi-x-circle me-2"></i>Cancelar
                    </button>
                    <button type="button" class="btn btn-danger" @onclick="EliminarLogo">
                        <i class="bi bi-trash me-2"></i>Sí, Eliminar
                    </button>
                </div>

                <div class="mt-3">
                    <a href="/" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i>Atrás
                    </a>
                </div>
            </div>
        </div>
    </div>
}

@code {
    private string logoActual = "/images/Logo.jpg";
    private string? previewImagen;
    private bool mostrarModalEliminar = false;
    private string? mensajeError;
    private string? mensajeExito;
    private bool isUploading = false;
    private string fileName = "";
    private string fileSize = "0";
    private Stream? archivoStream; // Cambié el nombre aquí
    private long fileLength = 0;

    protected override async Task OnInitializedAsync()
    {
        await CargarLogoActual();
    }

    private async Task CargarLogoActual()
    {
        try
        {
            logoActual = LogoService.ObtenerLogoActual();
        }
        catch (Exception ex)
        {
            mensajeError = $"Error al cargar logo: {ex.Message}";
        }
    }

    private async Task HandleFileSelected(ChangeEventArgs e)
    {
        try
        {
            mensajeError = null;
            mensajeExito = null;

            var input = e.Value as InputFileChangeEventArgs;
            if (input == null || input.FileCount == 0)
                return;

            var file = input.File; // 'file' es la variable correcta

            // Validaciones
            if (file.Size > 5 * 1024 * 1024)
            {
                mensajeError = "El archivo es demasiado grande (Máx. 5MB)";
                previewImagen = null;
                return;
            }

            // Guardar stream para luego usarlo
            fileLength = file.Size;
            fileName = file.Name;
            fileSize = (file.Size / 1024).ToString("N0");

            // Crear vista previa
            var buffer = new byte[file.Size];
            await file.OpenReadStream(maxAllowedSize: 5 * 1024 * 1024).ReadAsync(buffer);
            previewImagen = $"data:{file.ContentType};base64,{Convert.ToBase64String(buffer)}";

            // Guardar stream para subir
            archivoStream = new MemoryStream(buffer); // Usa archivoStream
        }
        catch (Exception ex)
        {
            mensajeError = $"Error al cargar archivo: {ex.Message}";
            previewImagen = null;
        }
    }

    private async Task SubirLogo()
    {
        if (archivoStream == null) // Cambié aquí
        {
            mensajeError = "Por favor selecciona un archivo";
            return;
        }

        isUploading = true;
        mensajeError = null;

        try
        {
            // Resetear posición del stream
            archivoStream.Position = 0; // Cambié aquí

            // Subir archivo usando el servicio
            var nuevaRuta = await LogoService.GuardarLogoAsync(archivoStream, fileName); // Cambié aquí

            // Actualizar logo actual
            logoActual = nuevaRuta;

            mensajeExito = "¡Logo subido exitosamente!";

            // Limpiar formulario
            ResetearFormulario();

            // Recargar después de 2 segundos
            await Task.Delay(2000);
            mensajeExito = null;

            // Forzar recarga de imagen (cache busting)
            await InvokeAsync(StateHasChanged);
        }
        catch (Exception ex)
        {
            mensajeError = $"Error al subir logo: {ex.Message}";
        }
        finally
        {
            isUploading = false;
            archivoStream?.Dispose(); // Cambié aquí
            archivoStream = null; // Cambié aquí
        }
    }

    private void ResetearFormulario()
    {
        previewImagen = null;
        fileName = "";
        fileSize = "0";
        fileLength = 0;
    }

    private void Cancelar()
    {
        ResetearFormulario();
        mensajeError = null;
        mensajeExito = null;
    }

    private void MostrarModalEliminar()
    {
        if (logoActual != "/images/Logo.jpg")
        {
            mostrarModalEliminar = true;
        }
    }

    private void CerrarModal()
    {
        mostrarModalEliminar = false;
    }

    private async Task EliminarLogo()
    {
        try
        {
            var resultado = LogoService.EliminarLogo();
            if (resultado)
            {
                logoActual = "/images/Logo.jpg";
                mensajeExito = "Logo eliminado, se restauró el logo por defecto";
                mostrarModalEliminar = false;

                await Task.Delay(2000);
                mensajeExito = null;
                await InvokeAsync(StateHasChanged);
            }
            else
            {
                mensajeError = "No se pudo eliminar el logo";
            }
        }
        catch (Exception ex)
        {
            mensajeError = $"Error al eliminar logo: {ex.Message}";
        }
    }
}